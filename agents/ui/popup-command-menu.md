# Command Menu Popup (`:CMakeMenu`, `:CMakeMenuFull`)

**Scope:** Command menu popup specifics.
**Trigger keywords:** command menu popup, Select command, CMakeMenu popup.
**Depends on:** `agents/ui/popups-shared.md`.
**Conflicts:** none.

1. Title starts as `Select command`.
2. All rows are numbered (`1.`, `2.`, ...).
3. Rows are command names with no current-item marker.
4. On confirm, executes selected command immediately (`silent` execution).

